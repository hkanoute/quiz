{% extends 'base.html.twig' %}

{% block title %}Play Quiz{% endblock %}

{% block body %}
    <h1>Play Quiz</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Submit</button>
    {{ form_end(form) }}
{% endblock %}
